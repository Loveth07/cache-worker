[project]
name = "cache-worker"
authors = ["PulseLink Engineering"]
description = "A decentralized cache management system for secure, permissioned data access"
telemetry = false
requirements = []
[contracts.cache-worker]
path = "contracts/cache-worker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
